<%= render 'remove_modal' %>

$('#goalsIndex').html('<%= j(render partial: "goals/goal", collection: current_user.goals, as: :goal) %>')

$("#modalArea").html("<%= j(render 'form', tag: Tag.new) %>")

<%= render 'focus_modal' %>

<%= render 'show_modal' %>

$("#modalArea").html("<%= j(render 'edit_form', tag: @tag) %>")

<%= render 'focus_modal' %>

<%= render 'show_modal' %>